# 使用教程（从零到跑通）

这份文档讲**怎么做**：每一步都给可复制的命令与真实输出。
想知道"这个项目是什么、有哪些能力、怎么实现的"，看 [`README.md`](README.md)。

> **先记住这一条**：项目的能力是**分层可用**的。
> 没有大模型 Key、没有 Docker、甚至没网，前几层照样能用起来——
> 别因为"缺依赖"就以为跑不起来，直接看 [第 0 节](#0-十分钟跑起来) 跑一次。

## 目录

- [0. 十分钟跑起来](#0-十分钟跑起来)
  - [0.5 全部功能地图（这个项目能做什么）](#05-全部功能地图这个项目能做什么)
- [1. 学生端：一次作业的完整流程](#1-学生端一次作业的完整流程)
  - [1.3 语言识别与代码结构（不用大模型）](#13-语言识别与代码结构不用大模型)
  - [1.10 界面上的其它实用功能（一次说全）](#110-界面上的其它实用功能一次说全)
  - [1.11 模型设置：在网页上选模型、填自己的 API Key](#111-模型设置在网页上选模型填自己的-api-key)
- [2. 整理一学期的作业](#2-整理一学期的作业)
  - [2.6 项目库的状态与上限](#26-项目库的状态与上限)
- [3. 看懂一个开源仓库（不需要 LLM / Docker）](#3-看懂一个开源仓库不需要-llm--docker)
  - [3.3 仓库管理：列表、详情、重新解析、支持的语言](#33-仓库管理列表详情重新解析支持的语言)
  - [3.4 检索参数与结果字段](#34-检索参数与结果字段)
- [4. 生成测试 → 自动修复 → PR 草稿](#4-生成测试--自动修复--pr-草稿)
  - [4.3 沙箱执行测试与覆盖率](#43-沙箱执行测试与覆盖率)
  - [4.4 LLM 配置状态接口](#44-llm-配置状态接口)
- [5. 配置与运行数据](#5-配置与运行数据)
  - [5.1 大模型：唯一必须自己填的](#51-大模型唯一必须自己填的)
  - [5.2 多模型：网页上切换模型 / 让使用者填自己的 Key](#52-多模型网页上切换模型--让使用者填自己的-key)
  - [5.3 检索、沙箱、仓库、项目库、分类的配置](#53-检索沙箱仓库项目库分类的配置)
  - [5.4 全部配置项速查表](#54-全部配置项速查表)
  - [5.5 运行数据都存在哪](#55-运行数据都存在哪)
  - [5.6 出问题怎么定位：Trace、日志与状态码](#56-出问题怎么定位trace日志与状态码)
- [6. 常见问题](#6-常见问题)
- [7. 开发与测试](#7-开发与测试)
- [8. 能力边界（如实说明）](#8-能力边界如实说明)

---

## 0. 十分钟跑起来

### 0.1 装 Python

只需要 **Python 3.11 或 3.12**，安装时**勾选 "Add Python to PATH"**。
依赖由脚本自动安装，你不用手动 `pip install`。

### 0.2 启动

先把代码拿到本机（已经拷到本地文件夹的，跳过这两行）：

```bash
git clone https://github.com/shulv2005/ai-code-tutor-agent.git
cd ai-code-tutor-agent
```

**方式 A（推荐，双击即可）**：

```
双击 start.bat  →  检查 Python → 建虚拟环境 → 装依赖 → 启动服务 → 打开浏览器
双击 stop.bat   →  关闭服务（释放 8000 端口）
```

**方式 B（想在命令行里调试）**：

```powershell
cd "D:\桌面\开源项目智能测试与贡献agent"

python -m venv .venv
.venv\Scripts\Activate.ps1          # Linux/macOS: source .venv/bin/activate
python -m pip install -r requirements.txt
Copy-Item .env.example .env         # Linux/macOS: cp .env.example .env
python -m uvicorn app.main:app --reload
```

`.env` 里**只有一项需要你自己填**（想用 AI 生成/修复功能的话）：

```ini
LLM__API_KEY=你的真实Key
```

默认留空是故意的——留空时服务会明确告诉你"未配置"，而不是拿着假 Key 在运行时 401。

### 0.3 确认服务活着

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/health
```

```json
{"status":"ok","service":"opensource-collab-agent","version":"0.1.0", ...}
```

三个地址记一下：

| 地址 | 用途 |
| --- | --- |
| <http://127.0.0.1:8000> | 学生端页面（会自动跳转） |
| <http://127.0.0.1:8000/docs> | 交互式接口文档：不确定怎么调接口时，点 "Try it out" 最省事 |
| <http://127.0.0.1:8000/api/v1/health> | 健康检查（响应头带 `X-Trace-Id`） |

> 页面必须通过这个地址访问，**不要双击 HTML 文件**——那样页面连不上后端。

### 0.4 缺什么会怎样（心里先有数）

| 能力 | 需要的条件 | 没有时会怎样 |
| --- | --- | --- |
| 浏览页面 / 项目库 / 文件分类 / 在线编辑 / 语言识别与代码结构 | 装好依赖即可 | — |
| AI 检测 / 改错 / 注释 | `.env` 里的 `LLM__API_KEY`，**或在网页「模型设置」里填自己的 Key** | 返回 **200** + 本地分析结论，`ai_available=false` 并说明原因 |
| 生成测试 / 自动修复 | `LLM__API_KEY`（这两个接口目前只认后端配置） | 接口返回 503，提示"未配置 LLM__API_KEY" |
| 建索引 / 语义检索 | 首次联网下载嵌入模型（约 180MB） | 自动降级为 `hashing` 嵌入（关键词匹配，精度低但可用） |
| 沙箱执行 | 有 Docker 最好 | 降级为本地子进程，**无隔离**，接口标注 `isolated: false` |

### 0.5 全部功能地图（这个项目能做什么）

下面按模块把项目里的**全部功能**列齐了。每条都写明「在哪用」和「详细步骤看哪一节」——
想不起来某个功能藏在哪、或者想找某个接口的用法，从这里查。

**A. 学生端页面（浏览器里点出来的）**

| # | 功能 | 在哪用 | 详细步骤 |
| --- | --- | --- | --- |
| A1 | 选模型、填自己的 API Key（保存并测试 / 清除 / 刷新模型清单） | 左侧「模型设置」卡片 | [1.11](#111-模型设置在网页上选模型填自己的-api-key) |
| A2 | 打开代码：拖拽 / 点选文件 / 粘贴 / `Ctrl+V` | 「① 选择你的代码」+ 中间代码区 | [1.2](#12-把代码放进来四种方式语义不同) |
| A3 | 自动识别语言 + 解析代码结构（函数/类清单、行数、圈复杂度；**不用大模型**） | 载入代码后左侧「③ 自动分类结果」 | [1.3](#13-语言识别与代码结构不用大模型) |
| A4 | AI 检测：0-100 分 + 等级 + 问题清单 + 做得好的地方 | 中间「🔍 AI 检测」 | [1.4](#14-ai-检测哪里有问题值多少分) |
| A5 | 生成注释：文件级 / 函数级 / 行内三层中文注释，并复核代码没被改动 | 中间「💬 生成注释」 | [1.5](#15-生成注释三层中文注释且不改你的代码) |
| A6 | 自动改错：修正后的代码 + 逐条四问讲解（错在哪 / 为什么 / 怎么改 / 以后怎么避免） | 中间「🛠 自动改错」 | [1.6](#16-自动改错给你改好的代码还讲清为什么) |
| A7 | 在线编辑：`Tab` 缩进、回车自动缩进、`Ctrl+S` 保存、`Esc` 放弃、行号同步 | 代码区「编辑 ✎」 | [1.7](#17-在线编辑与保存) |
| A8 | 替换 / 删除项目库里的文件（删除默认进回收站，可找回） | 项目库列表行上的按钮 | [1.8](#18-替换与删除) |
| A9 | 历史记录：每次分析 / 检测 / 注释 / 改错都留痕，点一条就把当时的代码载回来 | 左侧「④ 历史记录」 | [1.9](#19-历史记录) |
| A10 | 三条可拖动的分隔条、清空代码、确认弹窗、提示条、只读提示、大文件懒加载 | 全页面 | [1.10](#110-界面上的其它实用功能一次说全) |

**B. 本地项目库：把一学期的作业管起来**

| # | 功能 | 接口 / 配置 | 详细步骤 |
| --- | --- | --- | --- |
| B1 | 扫描配置好的文件夹，按语言分类并统计（语言 / 行数 / 体积） | `GET /api/v1/library/scan` | [2.1](#21-项目库扫描与筛选) |
| B2 | 按语言 / 根目录 / 关键词筛选 | 同上：`language` / `root` / `keyword` | [2.1](#21-项目库扫描与筛选) |
| B3 | 查看项目库配置与各项上限（前端据此显示只读模式与回收站名） | `GET /api/v1/library/status` | [2.6](#26-项目库的状态与上限) |
| B4 | 拖拽入库：文件存进项目库 + 按语言归档 | `POST /api/v1/files/upload` | [2.2](#22-文件自动分类把散放的作业归位) |
| B5 | 一键归档散放的作业（含 `dry_run` 预演） | `POST /api/v1/files/scan` | [2.2](#22-文件自动分类把散放的作业归位) |
| B6 | 归档台账（文件名 / 语言 / 路径 / 大小 / 入库时间 / 是否还在磁盘上） | `GET /api/v1/files/list` | [2.2](#22-文件自动分类把散放的作业归位) |
| B7 | 读取文件内容（返回 `sha256` 做乐观锁基准） | `GET /api/v1/library/file` | [1.7](#17-在线编辑与保存) |
| B8 | 在线编辑保存 / 用本机文件替换（同一个接口） | `PUT /api/v1/library/file` | [1.7](#17-在线编辑与保存) |
| B9 | 删除（默认进 `.trash/`，`permanent=true` 才是彻底删除） | `DELETE /api/v1/library/file` | [1.8](#18-替换与删除) |
| B10 | 只读模式：老师只收不改 | `LIBRARY__ALLOW_WRITE=false` | [2.5](#25-老师收作业的推荐流程) |

**C. 看懂一个开源仓库（不需要大模型、不需要 Docker）**

| # | 功能 | 接口 | 详细步骤 |
| --- | --- | --- | --- |
| C1 | 本环境真正支持解析哪些语言 | `GET /api/v1/repositories/languages` | [3.3](#33-仓库管理列表详情重新解析支持的语言) |
| C2 | 注册仓库：克隆 + AST / Tree-sitter 解析结构 | `POST /api/v1/repositories` | [3.1](#31-powershell) |
| C3 | 仓库列表（分页）与仓库详情 | `GET /api/v1/repositories`、`/{id}` | [3.3](#33-仓库管理列表详情重新解析支持的语言) |
| C4 | 看代码结构：文件 + 函数/类（可按语言、路径前缀过滤、分页） | `GET /api/v1/repositories/{id}/structure` | [3.1](#31-powershell) |
| C5 | 改了配置或解析器后重新解析（不重新克隆） | `POST /api/v1/repositories/{id}/reindex` | [3.3](#33-仓库管理列表详情重新解析支持的语言) |
| C6 | 建检索索引：BM25 + FAISS 向量 | `POST /api/v1/repositories/{id}/index` | [3.1](#31-powershell) |
| C7 | 混合检索代码：中文自然语言也能搜 | `POST /api/v1/search` | [3.4](#34-检索参数与结果字段) |

**D. 从 Issue 到 PR 草稿的完整链路**

| # | 功能 | 接口 / 命令 | 详细步骤 |
| --- | --- | --- | --- |
| D1 | 查 LLM 配置状态（前端据此提示"先配模型"） | `GET /api/v1/agent/status` | [4.4](#44-llm-配置状态接口) |
| D2 | 生成 pytest 测试（三种输入方式：代码片段 / 符号 ID / 中文描述检索） | `POST /api/v1/agent/generate_test` | [4.1](#41-生成测试的三种输入方式) |
| D3 | 查沙箱后端与安全配置（有没有真隔离） | `GET /api/v1/sandbox/status` | [4.3](#43-沙箱执行测试与覆盖率) |
| D4 | 在沙箱里跑测试并采集覆盖率 | `POST /api/v1/sandbox/run` | [4.3](#43-沙箱执行测试与覆盖率) |
| D5 | 一键自动修复：克隆 → 解析 → 建索引 → 定位 → 生成测试 → 执行 → 打补丁重跑 | `POST /api/v1/agent/auto_fix` | [4.2](#42-一键自动修复完整链路) |
| D6 | PR 草稿（标题 / 正文 / 分支名 / commit message）+ 可贴回 Issue 的评论 | 上面的 `pr_draft` / `issue_comment` | [4.2](#42-一键自动修复完整链路) |
| D7 | 一条命令看完整链路（离线演示 / 真实仓库两种跑法） | `python tests/e2e_test.py` | [4.5](#45-端到端脚本一条命令看完整链路) |

**E. 多模型与 API Key（让使用者自己带 Key）**

| # | 功能 | 接口 | 详细步骤 |
| --- | --- | --- | --- |
| E1 | 网页上选模型、填 Key、保存并测试、清除 | 「模型设置」卡片 | [1.11](#111-模型设置在网页上选模型填自己的-api-key) |
| E2 | 模型清单（ID / 展示名 / 提供商 / 是否需要 Key / 后端有没有配 Key） | `GET /api/v1/models/list` | [5.2](#52-多模型网页上切换模型--让使用者填自己的-key) |
| E3 | 把 Key 存进内存会话（返回 `session_id`） | `POST /api/v1/auth/set_key` | [5.2](#52-多模型网页上切换模型--让使用者填自己的-key) |
| E4 | 查会话状态（只回"有没有 Key"，不回 Key） | `GET /api/v1/auth/status` | [5.2](#52-多模型网页上切换模型--让使用者填自己的-key) |
| E5 | 清除 Key（清整个会话 / 只清某个模型） | `POST /api/v1/auth/clear_key` | [5.2](#52-多模型网页上切换模型--让使用者填自己的-key) |
| E6 | 有效期、会话上限、允许/禁止前端传 Key | `SECURITY__*` 三项配置 | [5.2](#52-多模型网页上切换模型--让使用者填自己的-key) |

**F. 运维：健康检查、数据、日志**

| # | 功能 | 位置 | 详细步骤 |
| --- | --- | --- | --- |
| F1 | 存活探针（服务在不在） | `GET /api/v1/health` | [0.3](#03-确认服务活着) |
| F2 | 就绪探针（连不连得上数据库） | `GET /api/v1/health/ready` | [5.6](#56-出问题怎么定位trace日志与状态码) |
| F3 | 交互式接口文档（不确定怎么调接口时用它） | <http://127.0.0.1:8000/docs> | [0.3](#03-确认服务活着) |
| F4 | 每次请求的链路号 `X-Trace-Id`（排查时按它串日志） | 所有响应头 + 返回体里的 `trace_id` | [5.6](#56-出问题怎么定位trace日志与状态码) |
| F5 | 运行数据都在哪（数据库 / 索引 / 仓库副本 / 沙箱 / 项目库 / 模型缓存） | `data/` 目录 | [5.5](#55-运行数据都存在哪) |
| F6 | 8 张业务表分别存什么 | `data/app.db` | [5.5](#55-运行数据都存在哪) |
| F7 | 一键启动 / 关闭（含依赖安装、端口检查、自动开浏览器） | `start.bat` / `stop.bat` | [0.2](#02-启动) |
| F8 | 常见报错与状态码对照 | — | [5.6](#56-出问题怎么定位trace日志与状态码) 与 [第 6 节](#6-常见问题) |

**G. 开发与验收（改完代码怎么确认没坏）**

| # | 功能 | 命令 | 详细步骤 |
| --- | --- | --- | --- |
| G1 | 单元 / 接口测试（1112 个用例，全部离线可跑） | `python -m pytest` | [7](#7-开发与测试) |
| G2 | 代码检查 | `python -m ruff check app tests tools` | [7](#7-开发与测试) |
| G3 | 16 个验收 / 审计脚本（会真的起服务发 HTTP 请求） | `python tools\verify_*.py` | [7](#7-开发与测试) |
| G4 | 前端静态交叉检查 + 真实行为测试（Node + DOM 桩，135 项） | `python tools\check_frontend.py` | [7](#7-开发与测试) |
| G5 | 死代码审计（没人引用的模块 / 函数 / CSS 类名 / 多余产物） | `python tools\audit_dead_code.py` | [7](#7-开发与测试) |
| G6 | 重新生成 `start.bat` / `stop.bat`（改脚本内容后） | `python tools\build_bat.py` | [7](#7-开发与测试) |

---

## 1. 学生端：一次作业的完整流程

### 1.1 认识界面

打开 <http://127.0.0.1:8000>，页面分三栏，最上面还有一条状态栏：

```
┌──────────────────────────────────────────────────────────────────────────┐
│ AI 代码导师          支持 C / Java / Python     AI 已就绪 · DeepSeek 官方  │ ← 两个状态徽章
├──────────────────────┬──────────────────────────────┬────────────────────┤
│ ⓞ 模型设置            │                              │                    │
│    选模型 / 填 Key     │      代码预览 / 在线编辑器     │   ⑤ 分析结果        │
│ ① 选择你的代码         │   [编辑✎][保存][取消][清空 ✕] │   · 评分 + 等级     │
│ ② 本地项目库           │                              │   · 问题清单        │
│ ③ 自动分类结果         │   [🔍 AI 检测][💬 生成注释]   │   · 做得好的地方     │
│ ④ 历史记录             │   [🛠 自动改错]               │   · 注释 / 改错结果  │
└──────────────────────┴──────────────────────────────┴────────────────────┘
        ↑ 可以拖动           ↑ 可以拖动（改代码区高度）
```

每个区域对应下面哪一节：

| 界面元素 | 作用 | 详细步骤 |
| --- | --- | --- |
| 顶部两个徽章 | 左边显示支持哪些语言，右边显示 AI 到底有没有就绪 | [1.11](#111-模型设置在网页上选模型填自己的-api-key) |
| ⓞ 模型设置 | 选模型、填自己的 API Key | [1.11](#111-模型设置在网页上选模型填自己的-api-key) |
| ① 选择你的代码 | 拖文件 / 点选文件 / 粘贴代码 | [1.2](#12-把代码放进来四种方式语义不同) |
| ② 本地项目库 | 作业入库、按语言筛选、替换、删除 | [1.2](#12-把代码放进来四种方式语义不同)、[1.8](#18-替换与删除)、[第 2 节](#2-整理一学期的作业) |
| ③ 自动分类结果 | 语言识别结果与函数/类结构 | [1.3](#13-语言识别与代码结构不用大模型) |
| ④ 历史记录 | 历次操作留痕，点一条载回当时的代码 | [1.9](#19-历史记录) |
| 中间代码区 | 代码高亮、在线编辑、三个 AI 按钮 | [1.7](#17-在线编辑与保存) |
| ⑤ 分析结果 | 检测报告 / 注释结果 / 改错对比 | [1.4](#14-ai-检测哪里有问题值多少分) 起三节 |

### 1.2 把代码放进来（四种方式，语义不同）

| 方式 | 怎么做 | 结果 |
| --- | --- | --- |
| 打开看看 | 拖到「① 选择你的代码」或点它选文件 | 载入预览，**不入库** |
| 交作业入库 | 拖到「② 本地项目库」的虚线框 | **入库** + 按语言归档 + 刷新列表 |
| 预览区拖入 | 拖到中间的代码区 | 载入预览，**不入库** |
| 粘贴 | 在中间区域直接 `Ctrl+V`，或用左侧「直接粘贴代码」 | 载入预览；文件名按内容猜（`pasted.py` / `pasted.c` / `Pasted.java`） |

「入库」和「看看」的区别在界面上写得很清楚，因为学生最容易搞混的就是"我到底交没交上"。
入库之后还可以继续改、替换、删除，见 [1.7](#17-在线编辑与保存) 与 [1.8](#18-替换与删除)。

### 1.3 语言识别与代码结构（不用大模型）

代码一载入，页面就会调一次 `POST /api/v1/tutor/analyze`：**只做本地解析，不调用任何大模型**。
所以这一步不需要 API Key、不花钱、断网也能用，几毫秒就出结果：

| 你能看到 | 在哪看 | 含义 |
| --- | --- | --- |
| 语言徽章 `Python` / `C` / `Java` | 代码区标题右边 | 按文件后缀 + 内容识别的语言 |
| 文件 / 语言 / 行数 / 字符 / 函数类数量 | 左侧「③ 自动分类结果」 | 一眼确认"系统读懂了我的文件" |
| 代码结构清单（`函数名 · function · L3-12`） | 上面卡片里的「查看代码结构」 | 每个函数/类的名字、类型、起止行号 |

命令行里等价的做法：

```powershell
$body = @{
  code     = "def add(a, b):`n    return a + b`n"
  filename = "add.py"
} | ConvertTo-Json

$r = Invoke-RestMethod "$Base/api/v1/tutor/analyze" -Method Post `
       -ContentType "application/json; charset=utf-8" -Body $body

"$($r.language_label)  $($r.line_count) 行  $($r.char_count) 字符"
$r.symbols | Format-Table qualified_name, kind, start_line, end_line, complexity
$r.imports                 # 文件里 import 了哪些模块
$r.parse_error             # 解析失败的原因；正常时是 null
$r.notes                   # 例如"语言不受支持，只能做通用检查"
```

也可以直接传文件（`multipart/form-data`，前端就是这么做的）：

```powershell
curl.exe -X POST "$Base/api/v1/tutor/analyze" -F "file=@data/library/python/homework1.py"
```

**三个要留意的细节**：

1. **识别的语言决定后面怎么检查**。后缀不在支持列表里时接口返回 `language=unknown`，
   前端会提示"无法识别代码语言"，三个 AI 功能也会拒绝——因为它们要知道语言才能提要求。
2. **有语法错误也能分析**：`parse_error` 会带上原因，结构清单可能是空的，
   但**不影响后面点「AI 检测」**（检测本来就是用来找这类问题的）。
3. **它也会记一条历史**：操作类型是「分析」，所以历史列表里能看到"我打开过哪些文件"。

### 1.4 AI 检测：哪里有问题、值多少分

点中间区域的「**AI 检测**」按钮即可。它会先本地查语法（精确到行列），再让 AI 查逻辑、边界与算法，
给出 0-100 评分、等级、问题清单（含行号与修改建议）和做得好的地方。

想在命令行里看完整结果，用深度检测接口：

```powershell
$Base = "http://127.0.0.1:8000"

$body = @{
  code     = @'
def average(scores):
    total = 0
    for i in range(len(scores) + 1):
        total += scores[i]
    return total / len(scores)
'@
  language = "python"
  filename = "average.py"
} | ConvertTo-Json

$r = Invoke-RestMethod "$Base/api/v1/check/code" -Method Post `
  -ContentType "application/json; charset=utf-8" -Body $body

$r.score; $r.level                     # 评分与等级
$r.errors | Format-Table line, severity, title
$r.style  | Format-Table line, title
$r.risks  | Format-Table line, title
$r.advice                               # 面向学生的学习建议
$r.local.syntax_error                   # 语法错误（含行列号与工具来源）
```

真实模型实测输出（`python tools\verify_check.py --ai`，12/12 通过）：

```
评分：55.0（待改进）  AI 参与：True
语法：通过
[错误/error/ai]      L3 循环多跑了一次，会越界
    → 去掉那个 +1：for i in range(len(scores)):，或者更简单地写成 for score in scores
[风格/info/ai]       L3 用下标循环不如直接遍历元素
[风险/warning/local] L8 collect 的默认参数用了可变对象
[风险/warning/local] L17 使用了裸 except
[风险/warning/ai]    L5 空列表会除以 0
[风险/warning/ai]    L15 打开的文件没有关闭
[风险/warning/ai]    L17 出错时返回 None，调用方容易踩空
学习建议：凡是做除法，先问一句「分母会不会是 0」；凡是打开文件，先想「谁来关它」，用 with 最省心。
```

注意每条都带 `[类别/严重程度/来源]`：`local` 是本地规则查出来的，`ai` 是模型查出来的，
学生能看出结论的依据是什么。

**三种语言都支持**：

| 语言 | 语法检查工具 | 能定位到 |
| --- | --- | --- |
| Python | 内置 `ast` | 行号 + 列号，例如 `SyntaxError: invalid syntax (line 1, column 7)` |
| C / Java | Tree-sitter | 行号 + 列号，例如"第 8 行附近有缺失的内容（语法错误）"（缺分号） |

**关于评分**：有语法错误时评分会被**硬性压到 45 分以内**（代码都跑不起来，不该因为模型心情好就给高分）；
没配模型时用本地规则打分，并封顶 85 分（本地规则看不出逻辑对错，不给"优秀"档）。

没配 `LLM__API_KEY` 时接口**依然返回 200**，只给本地结论（语法 + 风格 + 风险），
`ai_available=false`、`note` 里说明原因。

检测结果会写入 SQLite 表 `code_check_records`：

```powershell
python -c "import sqlite3; c=sqlite3.connect('data/app.db'); print(c.execute('select filename,language,score,level,error_count,style_count,risk_count from code_check_records').fetchall())"
```

### 1.5 生成注释：三层中文注释，且不改你的代码

点「**生成注释**」，或调接口：

```powershell
$body = @{
  code     = @'
def average(scores):
    total = 0
    for score in scores:
        total += score
    return total / len(scores)
'@
  language = "python"
  filename = "score.py"
} | ConvertTo-Json

$r = Invoke-RestMethod "$Base/api/v1/comment/generate" -Method Post `
       -ContentType "application/json; charset=utf-8" -Body $body

$r.commented_code                     # 带三层注释的完整代码
$r.verification.code_unchanged        # 代码逻辑有没有被改动（本地比对，不是模型自评）
$r.verification.functions_covered     # 有几个函数拿到了注释
$r.verification.inline_comment_lines  # 关键逻辑注释有几行
$r.verification.note                  # 人话总结
```

真实模型实测（`python tools\verify_comment.py --ai`，19/19 通过）：

```python
"""学生成绩统计：计算一组分数的平均分与最高分。"""


def average(scores):
    """求平均分。

    Args:
        scores: 分数列表

    Returns:
        所有分数的平均值
    """
    total = 0
    # 累加总分
    for score in scores:
        total += score
    # 求平均
    return total / len(scores)
```

C 与 Java 会按各自规范写：

```c
/* add.c
 * 演示如何定义并调用一个加法函数：把两个整数相加后输出结果。
 */
/*
 * 求两个整数之和。
 * 参数：a 第一个加数；b 第二个加数
 * 返回：a 与 b 相加的结果
 */
int add(int a, int b) { ... }
```

```java
/**
 * 这是一个简单的计算器类，目前只提供一个整数加法功能。
 */
public class Calculator {
    /**
     * 计算两个整数相加的结果。
     * @param a 第一个加数
     * @param b 第二个加数
     * @return 两个加数相加的和
     */
    public int add(int a, int b) { ... }
}
```

**为什么一定要看 `code_unchanged`**：注释类功能最危险的不是"注释写得不好"，
而是**模型顺手把你的代码改了**。本功能在本地做客观比对（Python 用 AST、C/Java 去掉注释后逐字符比）：

| 值 | 含义 |
| --- | --- |
| `true` | 除注释外代码完全一致，可以放心用 |
| `false` | 代码被改动了，接口同时把 `verified` 置为 false 并在 `warnings` 里点名 |
| `null` | 原代码本身有语法错误，无法比对（不会默认当成"没改"） |

原代码与带注释代码都会存库，随时回看：

```powershell
Invoke-RestMethod "$Base/api/v1/comment/history"
Invoke-RestMethod "$Base/api/v1/comment/history/1"
```

### 1.6 自动改错：给你改好的代码，还讲清为什么

点「**自动改错**」，或调接口：

```powershell
$body = @{
  code     = "def add(a, b)`n    return a + b`n"   # 注意：少了冒号
  language = "python"
  filename = "add.py"
} | ConvertTo-Json

$r = Invoke-RestMethod "$Base/api/v1/fix/code" -Method Post `
       -ContentType "application/json; charset=utf-8" -Body $body

$r.fixed_code                       # 修正后的完整代码，可直接复制运行
$r.verification.verified            # 本地复检：语法是否真的通过了
$r.verification.note                # 复检结论（含"逻辑无法本地验证"的边界说明）
$r.changes | Format-List what, why, how, avoid, original, fixed
$r.diff                             # 逐行对比
```

真实模型实测（`python tools\verify_fix.py --ai`，12/12 通过）：

```
发现错误：True  修改 1 处  类型统计：{'logic': 1}
总评：循环多跑了一轮，range 的上界写成了 len(scores) + 1，会访问到列表外面导致 IndexError。
本地复检：通过 —— 修改前后都能通过语法检查（这类问题通常是逻辑错误），
          **逻辑是否正确本地无法自动验证**：请自己造几组输入跑一遍。
── 第 1 处（logic，L3）
   错在哪：循环写成了 for i in range(len(scores) + 1)
   为什么：range 是左闭右开的，range(n) 只给出 0 到 n-1。这里 i 会取到 len(scores)，
           而最后一个合法下标是 len(scores) - 1，所以最后一轮会越界抛 IndexError。
   怎么改：把上界改回列表长度：for i in range(len(scores)):
   以后避免：写 range 时先想清楚要的是 0 到 n-1 还是 1 到 n——右端永远取不到。
```

**三个必须知道的点**：

1. **`verified` 只代表语法**。逻辑对不对本地验证不了，接口会在 `verification.note`
   里写明"请自己造几组输入跑一遍"。别把它当成"可以直接交作业"。
2. **模型给出仍有语法错误的代码时，复检会如实报出来**（`verified=false`），不会因为"是 AI 给的"就放行。
3. **原代码和新代码都会存库**，随时回看对比：

```powershell
Invoke-RestMethod "$Base/api/v1/fix/history"           # 历次修改列表
Invoke-RestMethod "$Base/api/v1/fix/history/1"         # 详情：原代码 / 新代码 / diff / 逐条讲解
```

### 1.7 在线编辑与保存

看完 AI 的结论，最自然的下一步就是"那我改一下"——不用切到别的编辑器再重新拖一遍。

点代码区标题栏的「**编辑 ✎**」进入编辑模式：

| 快捷键 / 操作 | 效果 |
| --- | --- |
| `Tab` | 插入 4 个空格（不会把焦点跳走） |
| 回车 | 自动补上当前行的缩进 |
| `Ctrl+S` | 保存（拦掉了浏览器自带的"保存网页"） |
| `Esc` | 放弃修改（有未保存内容时会先确认） |
| 左侧行号 | 跟着滚动同步——AI 说的"第 8 行"能直接对上 |

**保存落到哪里，取决于这段代码是从哪来的**：

| 代码来源 | 点「保存」会发生什么 |
| --- | --- |
| 项目库里的文件（点列表载入、或拖进 ② 入库） | **真的写回磁盘**，并刷新项目库列表 |
| 拖进 ①/③ 只看一眼的、粘贴的代码 | 只更新预览，提示「这段代码不在项目库里，没有写进任何文件」 |

**边改边测**：编辑模式下三个 AI 按钮照常可用，分析的是"你正在写的这一版"，
不用先保存就能看评分变化——适合"改一行 → 点检测 → 看分数有没有变好"的循环。

几个如实说明的细节：

- **换行符保留原样**：原来是 CRLF 的文件保存后还是 CRLF，不会因为改一行就整份变样；
- **编码会统一成 UTF-8**：原文件是 GBK（Windows 记事本常见）时中文不会乱，但编码会变，提示里会说明；
- **写入是原子的**：先写同目录临时文件再替换，中途失败也不会留下半截作业；
- **文件被别处改过会拒绝保存**：打开时页面记下了内容指纹（`sha256`），
  你又在记事本里改了同一个文件，再点保存会返回 **409** 并让你重新打开——
  这是故意的，免得把你后来的改动盖掉。

### 1.8 替换与删除

鼠标移到「② 本地项目库」列表里的某个文件上，会出现两个按钮：

| 按钮 | 做什么 | 注意 |
| --- | --- | --- |
| **替换** | 选本机另一个文件，用它覆盖项目库里这个路径 | 必须**同扩展名**（`.c` → `.c`）；新增文件请拖到虚线框入库 |
| **删除** | 把文件移进项目库文件夹下的 `.trash/` | 不是真删，可找回；页面上没有"一键永久删除"的入口 |

两个操作都会先弹确认框，把**具体是哪个文件**写清楚，确定按钮是红色的。

**删掉的文件去哪了**：`data/library/.trash/`，名字形如
`20260218-153000__python__homework1.py`（时间戳 + 原相对路径）。
把名字改回 `homework1.py`、移出 `.trash` 就恢复了。

**想让学生只能看不能改**：在 `.env` 里设 `LIBRARY__ALLOW_WRITE=false`，
页面上这三个入口会直接消失，项目库卡片里会写明"当前是只读模式"。

命令行里等价的做法：

```powershell
# 读取（返回里带 sha256，保存时要带回去做乐观锁）
Invoke-RestMethod "$Base/api/v1/library/file?path=python/homework1.py"

# 保存 / 替换
$body = @{ path = "python/homework1.py"; code = "print('改过了')`n" } | ConvertTo-Json
Invoke-RestMethod "$Base/api/v1/library/file" -Method Put `
  -ContentType "application/json" -Body $body

# 删除（默认进回收站）
Invoke-RestMethod "$Base/api/v1/library/file?path=python/homework1.py" -Method Delete
```

### 1.9 历史记录

**四种操作**都会写进「④ 历史记录」：打开文件（分析）、AI 检测、生成注释、自动改错。
列表里每条显示文件名、操作类型（检测还会显示分数）和语言、时间，**点一下就把当时的原始代码载回编辑区**——
所以"上次那份作业"永远找得回来。

```powershell
Invoke-RestMethod "$Base/api/v1/tutor/history"                        # 最近 20 条
Invoke-RestMethod "$Base/api/v1/tutor/history?limit=50&offset=50"     # 翻页（limit 上限 100）
Invoke-RestMethod "$Base/api/v1/tutor/history?action=check"           # 只看检测记录
Invoke-RestMethod "$Base/api/v1/tutor/history?filename=score.py"      # 只看某个文件
Invoke-RestMethod "$Base/api/v1/tutor/history/1"                      # 详情（含当时的完整代码与完整结果 JSON）
Invoke-RestMethod "$Base/api/v1/tutor/history/1" -Method Delete       # 删除一条
```

三个深浅不同的历史各自独立，按需取用：

| 历史 | 存了什么 | 接口 |
| --- | --- | --- |
| 学习历史（页面上这个） | 文件名、语言、操作、评分、当时的代码 | `/api/v1/tutor/history` |
| 检测历史（深度版） | 四类问题、评分明细、语法错误、指标 | `/api/v1/check/code` 的结果落 `code_check_records` |
| 改错 / 注释历史 | 原代码 + 新代码 + diff + 逐条讲解 | `/api/v1/fix/history`、`/api/v1/comment/history`（都支持 `language` / `filename` 筛选） |

> 页面上只做了「载入」和「刷新」，**没有做删除按钮**——删除是不可逆操作，
> 放在学生随手能点到的地方不合适。需要清理时用上面的 `-Method Delete` 或直接改数据库。

### 1.10 界面上的其它实用功能（一次说全）

这一节把界面上那些"用得到但没单独讲"的功能一次说完：

**① 三条可拖动的分隔条**（鼠标悬停会变成 ↔ / ↕ 光标）

| 分隔条 | 拖动效果 | 键盘也能调 |
| --- | --- | --- |
| 左栏与中间之间 | 调整「左侧栏」宽度 | 聚焦后用 `←` `→` |
| 中间与右栏之间 | 调整「右侧结果区」宽度 | 聚焦后用 `←` `→` |
| 代码区下面那条横的 | 调整代码预览区高度（往下拖 = 代码区变高） | 聚焦后用 `↑` `↓` |

拖动时会自动守边界：左侧栏最小 200px、中间代码区最小 400px、右侧结果区最小 280px、
代码区最小 120px；任何一侧最多占 60%，**中间栏永远不会被挤没**。
调好的尺寸记在浏览器里（`localStorage` 的 `ai-tutor.layout`），刷新后还在；
窗口宽度小于 1280px 时自动收起固定宽度、回到自适应布局（免得小屏上左右栏把代码区挤没）。
注意这里存的是**面板尺寸**，API Key 永远不会写进 localStorage，见 [1.11](#111-模型设置在网页上选模型填自己的-api-key)。

**② 代码区标题栏的按钮**

| 按钮 | 作用 |
| --- | --- |
| `编辑 ✎` | 切到编辑模式（见 [1.7](#17-在线编辑与保存)） |
| `保存` / `取消` | 只在编辑模式下出现 |
| `清空 ✕` | 清掉当前代码与右侧结果，"还没有代码"的占位提示会回来 |
| `未保存` 徽章 | 只要改过内容就亮起——避免你以为自动保存了、刷新后才发现白写 |

**③ 键盘快捷键**

| 快捷键 | 位置 | 效果 |
| --- | --- | --- |
| `Ctrl+V` | 代码区 | 直接粘贴剪贴板里的代码（文件名按内容猜） |
| `Tab` | 编辑器 | 插入 4 个空格，不跳走焦点 |
| 回车 | 编辑器 | 自动补上当前行的缩进 |
| `Ctrl+S` | 编辑器 | 保存（拦掉了浏览器自带的"保存网页"） |
| `Esc` | 编辑器 | 放弃修改（有未保存内容会先确认） |
| `←` `→` `↑` `↓` | 分隔条 | 调面板尺寸（先按 Tab 聚焦到分隔条） |
| `Esc` | 确认弹窗 | 等同于点「取消」 |

**④ 提示与确认**

- **提示条（toast）**：右下角/顶部一闪而过的提示，分「成功 / 失败 / 提示」三种。
  例如「已识别为 Python」「这段代码不在项目库里，没有写进任何文件」「Key 已过期或被清除，请重新填写后再试」。
- **确认弹窗**：替换文件、删除文件、放弃未保存的修改都会弹出来，里面写清**具体是哪个文件**，
  确定按钮是红色的。没有用浏览器原生 `confirm()`，所以窗口不会冻住、样式也可控。

**⑤ 几种"如实告诉你"的状态**

| 你会看到 | 意思 | 怎么处理 |
| --- | --- | --- |
| 项目管理库卡片里的「当前是只读模式」 | 后端配了 `LIBRARY__ALLOW_WRITE=false` | 想让学生能改，把它改成 `true` 并重启 |
| 「文件太多，只列出了前 N 个」 | 扫描文件数触到上限 `LIBRARY__MAX_FILES` | 调大上限，或把作业分目录放 |
| 某个文件显示「文件过大，未统计行数」 | 超过 `LIBRARY__MAX_FILE_BYTES`（默认 500KB） | 只登记元信息，仍可点开看/改 |
| 路径显示成醒目的缺失样式 | 配置的目录不存在 | 按页面显示的路径把文件夹建出来 |

**⑥ 大文件不卡**：代码高亮是分批做的（几十 KB 以上的文件先显示出来、再逐段上色），
所以拖进一个几 MB 的作业页面也不会卡住——这是有自动化断言在管的行为。

### 1.11 模型设置：在网页上选模型、填自己的 API Key

这是"让使用者自己带 Key"的入口，也是全班共用一台服务器时最实用的一个功能：
**老师不用把自己的 Key 写进 `.env` 发给所有人，每个同学在页面上填自己的 Key 就行。**

页面上四步走：

| 步骤 | 操作 | 看到什么 |
| --- | --- | --- |
| 1 | 打开左侧「模型设置」，「使用哪个模型」下拉框里选一个 | 后端 `.env` 里配了几个模型，这里就有几个（本地 Ollama 会标注"免 Key"）；**只有一个**时卡片下方会写明原因，见下面的说明 |
| 2 | 在「我的 API Key」里粘贴自己的 Key（点「显示」可以核对有没有粘错） | 输入框默认是密码样式，`autocomplete=off`，浏览器不会记住 |
| 3 | 点「**保存并测试**」 | 它**会真的拿这把 Key 调一次 AI 检测**，成功才说"已连接"——只保存不测试的话，Key 写错了要等学生点检测才发现 |
| 4 | 右上角徽章变成绿色的「**AI 已就绪 · 模型名**」 | 三个 AI 按钮从灰变亮，可以开始用了 |

> **下拉框里只有一个模型（比如只有 DeepSeek）？** 说明 `.env` 里没写多模型那一段，
> 页面显示的是"单模型写法"的兜底项——卡片下方的说明文字会直接点出这一点，
> 并告诉你该加 `LLM__MODELS__<ID>__*`。加完重启服务、再点 ↻ 就有了，
> 完整写法见 [5.2](#52-多模型网页上切换模型--让使用者填自己的-key)。

想换模型、换 Key：直接改下拉框、重新填一次即可；
点「**清除 Key**」= 退出（会话与 Key 一起删掉，徽章回到「AI 未启用」）；
点标题右边的「**↻**」= 重新读一次模型清单（后端刚改了 `.env` 时用）。

**右上角徽章只有三种状态**，看到哪种就知道处在哪一步：

| 徽章 | 含义 | 怎么办 |
| --- | --- | --- |
| `AI 未启用`（灰色） | 后端没配 Key，页面上也没填 | 填 Key 点「保存并测试」；或在 `.env` 里配好 Key |
| `AI 已就绪 · deepseek-chat`（绿色） | 后端 `.env` 里配好了，开箱即用 | 不用做任何事 |
| `AI 已就绪 · DeepSeek 官方`（绿色） | 你在页面上填的 Key 已生效 | — |

**四条必须知道的限制**（都是刻意设计，不是 bug）：

1. **Key 只活在内存里**：刷新或关闭页面后要重新填（前端不写 `localStorage`），
   后端进程重启也会清空。
2. **30 分钟不操作会自动失效**（每次使用顺延），失效后再点 AI 功能会提示
   「Key 已过期或被清除，请重新填写后再试」，徽章同时回到未启用状态。
3. **后端没配模型清单时**，下拉框里只有一个「默认模型」（来自 `LLM__MODEL`），
   仍然可以填 Key 使用。
4. **老师可以关掉这个入口**：`SECURITY__ALLOW_CLIENT_KEY=false` 后，
   页面上填 Key 会被 403 拒绝，只能用 `.env` 里配好的那把——适合"统一由老师出 Key"的课堂。

接口版的等价做法（脚本 / 命令行 / 自己写前端时用）见 [5.2](#52-多模型网页上切换模型--让使用者填自己的-key)。

---

## 2. 整理一学期的作业

### 2.1 项目库：扫描与筛选

页面上「② 本地项目库」会自动扫描配置好的目录，按语言分类并显示每个语言的文件数。
**卡片里一直显示文件夹的完整路径**，点旁边的「复制」就能粘到文件资源管理器里打开。

```powershell
Invoke-RestMethod "$Base/api/v1/library/scan"                        # 全部
Invoke-RestMethod "$Base/api/v1/library/scan?language=python"        # 只看 Python
Invoke-RestMethod "$Base/api/v1/library/scan?root=library"           # 只看某个根目录（多个根目录时用）
Invoke-RestMethod "$Base/api/v1/library/scan?keyword=homework"       # 按路径做包含匹配（忽略大小写）
Invoke-RestMethod "$Base/api/v1/library/status"                      # 配置与上限
```

返回里直接给你能用的统计：`roots`（每个根目录的绝对路径与文件数）、
`files`（`rel_path` / 语言 / 行数 / 字节数 / 修改时间 / `too_large` 标记）、
`language_counts` + `language_labels`（前端据此生成语言筛选按钮）、
`total_files` / `total_lines` / `total_bytes`、`truncated`（是否触到文件数上限）、
`allow_write` 与 `trash_dir_name`（前端据此决定显不显示「替换 / 删除」）。

**默认扫描哪里**：项目根目录下的 `data/library/`，外加随项目附带的 `examples/`
（里面是故意写错的示例作业，第一次打开就有东西可点）。

### 2.2 文件自动分类：把散放的作业归位

如果作业是**散着放**的（根目录、子文件夹里到处都有 `.c`/`.java`/`.py`），用这个功能一键归档：

```powershell
# 1) 先预演：只告诉你"打算把谁移到哪"，不动文件、不写库（强烈建议第一次这么用）
Invoke-RestMethod "$Base/api/v1/files/scan" -Method Post `
  -ContentType "application/json" -Body '{"dry_run": true}'

# 2) 正式扫描：真的归档 + 写入 SQLite 台账
Invoke-RestMethod "$Base/api/v1/files/scan" -Method Post `
  -ContentType "application/json" -Body '{}'

# 3) 按语言查看台账
Invoke-RestMethod "$Base/api/v1/files/list"
Invoke-RestMethod "$Base/api/v1/files/list?language=c"
```

实测输出（预演）：

```
source_path -> target_path (语言, 动作)
main.c                -> c/main.c                   (C, planned)
utils.h               -> c/utils.h                  (C, planned)
homework/sort.py      -> python/sort.py             (Python, planned)
readme.txt            -> readme.txt                 (未知, kept)
```

实测输出（台账）：

```
#1 main.c           C      c/main.c            48B  入库=2026-02-18T10:33:02  在磁盘上=True
#2 readme.txt       未知    readme.txt          28B  入库=2026-02-18T10:33:02  在磁盘上=True
#3 sort.py          Python python/sort.py      32B  入库=2026-02-18T10:33:02  在磁盘上=True
```

归档规则：`.c` / `.h` → `c/`，`.java` → `java/`，`.py` → `python/`，其它默认只登记不移动。

### 2.3 三条"不会把作业弄丢"的保证

1. **绝不覆盖同名文件**：两个子文件夹里各有一个 `main.c` 时，第二个会变成 `main_1.c`，
   接口返回的 `note` 里会写明"已被重命名"。
2. **可以反复点**：归档目录 `c/` `java/` `python/` `unknown/` 与回收站 `.trash/`
   在扫描时都会被跳过，所以连点两次不会出现 `c/c/main.c` 这种套娃，也不会产生重复记录。
3. **未知文件默认不动**：`.txt`、`.md`、`.go` 只登记、不移动（它们可能不是作业）。
   想让它们也归到 `unknown/`，把请求体写成 `{"move_unknown": true}`，
   或把 `.env` 里的 `CLASSIFIER__MOVE_UNKNOWN` 改成 `true`。

另外：软链接会跳过、单文件超过 5MB 会跳过并说明原因、
接口只允许扫描 `CLASSIFIER__ROOT` 及其子目录（传 `../..` 会返回 400）。

### 2.4 换成自己的作业目录

在 `.env` 里改（改完重启服务）：

```ini
# 左侧「② 本地项目库」列表从哪扫描（读写都作用在这些目录里）
LIBRARY__ROOTS=D:\我的作业, E:\课程代码

# 文件自动分类与拖拽入库把文件放到哪（会真的写入磁盘）
CLASSIFIER__ROOT=D:\我的作业
```

**能被扫描的目录只能在 `.env` 里配置**，页面上没有任何入口能指定任意路径；
读和写都会校验目标确实落在根目录内，`../../` 这类路径穿越与指向外部的软链接都会被拒绝（返回 400）。

### 2.5 老师收作业的推荐流程

1. 把 `LIBRARY__ROOTS` 与 `CLASSIFIER__ROOT` 都指向班级作业目录；
2. 学生打开页面 → 拖文件到「② 本地项目库」→ 入库；
3. 想按语言整理就点一次归档（或让学生先自己 `dry_run` 看一眼）；
4. 需要"只收不改"时设 `LIBRARY__ALLOW_WRITE=false`；
5. 期末清理：`data/library/.trash/` 里的都可以直接删（那是学生自己删掉的）。

### 2.6 项目库的状态与上限

`.env` 配完不确定生效没有，或者想知道"为什么某个文件没被扫到"，看这两个接口：

```powershell
$s = Invoke-RestMethod "$Base/api/v1/library/status"
$s.default_path            # 默认项目库目录（页面卡片里显示的就是它）
$s.roots                   # 参与扫描的每个目录：路径 + 是否存在
$s.supported_languages     # 能识别哪些语言
$s.allow_write             # 现在是可写还是只读
$s.trash_dir_name          # 回收站目录名
"$($s.max_files) 个文件 / 深度 $($s.max_depth) / 单文件 $($s.max_file_bytes) 字节"
```

| 字段 / 配置 | 默认值 | 超了会怎样 |
| --- | --- | --- |
| `LIBRARY__MAX_FILES` | 500 | 扫描提前停止，返回 `truncated=true`，页面上写"只列出了前 N 个" |
| `LIBRARY__MAX_DEPTH` | 6 | 更深的目录不再递归（防止软链接成环把扫描拖死） |
| `LIBRARY__MAX_FILE_BYTES` | 512000 | 只登记元信息不读内容（列表标「文件过大，未统计行数」），读取时返回 **413** |
| `LIBRARY__INCLUDE_EXAMPLES` | true | 设 `false` 就不再扫描自带的 `examples/` 示例目录 |
| `LIBRARY__ALLOW_WRITE` | true | 设 `false` → 编辑/替换/删除全部禁用：接口返回 **403**，页面上按钮直接消失 |
| `LIBRARY__TRASH_DIR_NAME` | `.trash` | 设成空字符串 = 删除时不留回收站，直接彻底删除 |

状态接口**刻意不做文件统计**（只回答"目录在哪、存不存在、上限多少"），
因为页面加载时本来就会调一次 `/scan`，没必要把磁盘扫两遍。

---

## 3. 看懂一个开源仓库（不需要 LLM / Docker）

以 requests 仓库为例。**注意顺序**：先注册仓库，再建索引，才能检索。

### 3.1 PowerShell

```powershell
$Base = "http://127.0.0.1:8000"

# 1) 注册仓库：克隆 + 解析代码结构
$r = Invoke-RestMethod "$Base/api/v1/repositories" -Method Post `
       -ContentType "application/json" `
       -Body (@{ url = "https://gitee.com/mirrors/requests.git" } | ConvertTo-Json)

"状态=$($r.repository.status)  文件=$($r.stats.file_count)  符号=$($r.stats.symbol_count)"
# 输出: 状态=ready  文件=36  符号=743

# 2) 看代码结构（文件 + 函数/类，含行号与圈复杂度）
$s = Invoke-RestMethod "$Base/api/v1/repositories/1/structure"
$s.languages                                  # 语言分布
$s.files[0].symbols | Select-Object qualified_name, kind, start_line, complexity

# 3) 建检索索引
$i = Invoke-RestMethod "$Base/api/v1/repositories/1/index" -Method Post
"分块=$($i.chunk_count)  维度=$($i.dimension)  嵌入后端=$($i.embedder)"

# 4) 用中文搜代码
$q = Invoke-RestMethod "$Base/api/v1/search" -Method Post `
       -ContentType "application/json" `
       -Body (@{ repository_id = 1; query = "怎么检查状态码并抛异常"; top_k = 3 } | ConvertTo-Json)
$q.hits | Format-Table qualified_name, path, start_line, end_line
```

### 3.2 curl（Linux / macOS / Git Bash）

```bash
Base=http://127.0.0.1:8000

curl -X POST "$Base/api/v1/repositories" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://gitee.com/mirrors/requests.git"}'

curl -X POST "$Base/api/v1/repositories/1/index"

curl -X POST "$Base/api/v1/search" \
  -H "Content-Type: application/json" \
  -d '{"repository_id": 1, "query": "怎么检查状态码并抛异常", "top_k": 3}'
```

> ⚠️ **Windows 上用 `curl.exe` 会踩坑**：PowerShell 5.1 向原生命令传参时会吃掉 JSON 里的内层双引号，
> 于是服务端收到 `{url: ...}` 并报 `JSON decode error`。
> **Windows 请用 `Invoke-RestMethod`**（如上），或改用 `curl.exe --data-raw` 并把 JSON 写进文件，
> 也可以直接用 `/docs` 页面。

### 3.3 仓库管理：列表、详情、重新解析、支持的语言

注册过的仓库会一直留在数据库里（副本在 `data/repos/`），下次不用重新克隆：

```powershell
# 1) 本环境真正能解析哪些语言（取决于语法包装没装上）
(Invoke-RestMethod "$Base/api/v1/repositories/languages").languages

# 2) 仓库列表（分页，limit 上限 200）与单个仓库详情
$list = Invoke-RestMethod "$Base/api/v1/repositories?limit=20&offset=0"
"共 $($list.total) 个仓库"
$list.items | Format-Table id, name, status, file_count, symbol_count, local_path
Invoke-RestMethod "$Base/api/v1/repositories/1"

# 3) 仓库代码结构：分页 + 按语言 / 路径前缀过滤
Invoke-RestMethod "$Base/api/v1/repositories/1/structure?language=python&path_prefix=src/&limit=50"

# 4) 改了 .env（比如换了排除目录）或换了解析器后：只重新解析，不重新克隆
Invoke-RestMethod "$Base/api/v1/repositories/1/reindex" -Method Post

# 5) 建索引（force=true 忽略已有索引强制重建）
Invoke-RestMethod "$Base/api/v1/repositories/1/index" -Method Post
Invoke-RestMethod "$Base/api/v1/repositories/1/index?force=true" -Method Post
```

三个常见返回值的含义：

| 返回 | 意思 | 怎么办 |
| --- | --- | --- |
| `404 仓库不存在` | ID 写错了 | 先调仓库列表拿真实 ID |
| `400` + 主机不在白名单 | 想克隆的域名不在 `REPOSITORY__ALLOWED_HOSTS` | 把域名加进 `.env` 再重启（这是挡 SSRF 的闸门） |
| `reindex` 返回 **409** | 本地副本被删掉了（`data/repos/` 被清理过） | 按提示重新注册并传 `force=true`：`@{url=...; force=$true}` |

注册时还能传 `branch` 指定分支、传 `force=true` 覆盖同名仓库重新克隆。

### 3.4 检索参数与结果字段

`POST /api/v1/search` 是"用中文找代码"的关键接口，参数不多但都很有用：

| 参数 | 说明 |
| --- | --- |
| `repository_id` | 必填，目标仓库 |
| `query` | 必填，自然语言或代码片段（中文、英文、标识符混着写都行） |
| `top_k` | 返回几条，1-100，缺省用 `RETRIEVAL__TOP_K`（默认 10） |
| `language` | 只搜某个语言，如 `python` |
| `include_tests` | 是否把测试代码也搜出来；缺省会把测试符号排到实现代码之后 |

```powershell
$q = Invoke-RestMethod "$Base/api/v1/search" -Method Post -ContentType "application/json" `
  -Body (@{ repository_id = 1; query = "检查状态码并抛异常"; top_k = 5; language = "python" } | ConvertTo-Json)

$q.total                       # 命中总数
$q.stale                       # 索引是否落后于仓库最新提交（true = 该重建索引了）
$q.timings_ms                  # 各阶段耗时：检索慢在哪一目了然
$q.notes                       # 降级说明（比如"嵌入模型不可用，已用 hashing 兜底"）
$q.hits | Format-Table path, qualified_name, start_line, end_line, score
$q.hits[0].code                # 源码片段，可以直接喂给生成测试 / 自动修复
$q.hits[0].bm25_score, $q.hits[0].vector_score, $q.hits[0].bm25_rank, $q.hits[0].vector_rank
```

排序分 `score` 是 **RRF 融合分**（把 BM25 关键词分和向量语义分按名次融合），
量纲无关、只用于排序，所以不同查询之间**不要比大小**；
想看"到底是关键词命中还是语义命中"，看后面两列 `bm25_rank` / `vector_rank` 更直观。

**想搜得更准**（实测经验，见第 6 节的两条 FAQ）：

- 查询里带上英文标识符：`raise_for_status 检查状态码` 明显好于纯中文；
- 代码里的专有名词（类名、函数名、常量名）直接照抄；
- 跨语言要求高时换更大的嵌入模型（`RETRIEVAL__EMBEDDING_MODEL`）。

---

## 4. 生成测试 → 自动修复 → PR 草稿

### 4.1 生成测试的三种输入方式

```powershell
# 方式 1：直接贴代码片段
$body = @{
  code        = "def add(a: int, b: int) -> int:`n    return a + b`n"
  file_path   = "pkg/core.py"
  symbol_name = "add"
} | ConvertTo-Json
$t = Invoke-RestMethod "$Base/api/v1/agent/generate_test" -Method Post `
       -ContentType "application/json" -Body $body

$t.test_functions        # 生成的测试函数名
$t.test_code             # 完整测试代码
$t.saved_path            # 落盘位置
```

```powershell
# 方式 2：从仓库检索定位（给它一句中文描述）
$body = @{ repository_id = 1; query = "检查状态码并抛异常" } | ConvertTo-Json

# 方式 3：直接指定符号 ID（从 /structure 里拿到的 id）
$body = @{ repository_id = 1; symbol_id = 123 } | ConvertTo-Json
```

生成的测试会写到 `data/workspaces/<run_id>/sandbox_repo/tests/test_generated.py`。
指定了 `repository_id` 时，沙箱目录里会**一并放入仓库副本**，
所以生成的测试能 `from calc import add` 导入真实模块——这个目录可以直接交给下一步执行。

### 4.2 一键自动修复（完整链路）

这是把前面所有能力串起来的一条命令：

```powershell
$body = @{
  repository_url = "https://gitee.com/mirrors/requests.git"
  issue          = "取消订单时没有校验状态，已取消的订单还能再次取消"
  max_attempts   = 3
} | ConvertTo-Json

$fix = Invoke-RestMethod "$Base/api/v1/agent/auto_fix" -Method Post `
         -ContentType "application/json" -Body $body
```

返回里能拿到全过程的每一步：

```powershell
$fix.status              # passed / max_attempts / no_patch / not_patchable / patch_rejected / no_progress
$fix.success             # 是否收敛
$fix.iterations          # 每一轮：用例数、失败归类、补丁是否应用
$fix.final_diff          # 真实代码改动（git diff）
$fix.changed_files
$fix.coverage_percent
$fix.pr_draft.title      # PR 标题
$fix.pr_draft.body       # PR 正文（Markdown，可直接贴到 GitHub/Gitee）
$fix.pr_draft.branch_name
$fix.issue_comment       # 可以贴回 Issue 的评论
```

**安全说明**：所有补丁只打在 `data/workspaces/<run_id>/sandbox_repo` 这个**隔离副本**上，
你克隆的仓库全程只读。副本自带 git 基线，可随时丢弃。

### 4.3 沙箱执行测试与覆盖率

生成的测试到底过不过？把它交给沙箱接口执行就知道了。先看沙箱现在是什么状态：

```powershell
$st = Invoke-RestMethod "$Base/api/v1/sandbox/status"
"后端=$($st.backend)  有隔离=$($st.isolated)  镜像=$($st.image)  镜像已拉取=$($st.image_present)"
$st.reason                 # 为什么降级成 local（例如"Docker daemon 未运行"）
$st.warnings               # 需要提醒你的地方
$st.limits                 # 超时 / 内存 / CPU / 进程数 / 输出上限
$st.security | Format-List # 断网、只读根文件系统、cap_drop、有没有挂 docker.sock 等
```

然后执行：

```powershell
$body = @{
  workspace        = "1a2b3c4d5e6f7788/sandbox_repo"   # 上一步生成测试时返回的 sandbox_dir（相对沙箱工作区）
  test_target      = "tests"                            # pytest 目标（相对 workspace）
  coverage_enabled = $true
} | ConvertTo-Json

$run = Invoke-RestMethod "$Base/api/v1/sandbox/run" -Method Post `
         -ContentType "application/json" -Body $body

"退出码=$($run.exit_code)  通过=$($run.passed)  失败=$($run.failed)  错误=$($run.errors)"
"覆盖率=$($run.coverage.percent_covered)%  $($run.coverage.covered_lines)/$($run.coverage.num_statements) 行"
$run.stdout                              # pytest 的输出
$run.coverage.files | Format-Table path, percent_covered, missing_lines
$run.notes                               # 降级说明，例如"本地后端不具备隔离能力…"
```

| 返回字段 | 含义 |
| --- | --- |
| `backend` / `isolated` | `docker` = 真隔离；`local` = 本地子进程，**没有隔离能力** |
| `exit_code` / `succeeded` / `timed_out` | pytest 退出码；0 才算通过，超时会单独标出来 |
| `passed` / `failed` / `errors` | 用例统计（`errors` 通常是 collection error，比如 import 不到模块） |
| `coverage` | 覆盖率汇总 + 每个文件的明细（`files` 最多返回 100 个） |
| `truncated` | 输出超过 `DOCKER__MAX_OUTPUT_BYTES` 被截断了 |
| `command` / `container_id` | 实际执行的命令与容器 ID（排查用） |

**四条安全边界**（这部分是刻意的设计，不是"限制太死"）：

1. `workspace` **必须落在** `DOCKER__WORKSPACE_DIR`（默认 `data/workspaces/`）**之内**，
   传 `../../` 或指向外部的软链接一律返回 **400**——否则这个接口就成了读取宿主机任意目录的后门；
2. `command` 自定义命令**只在 Docker 后端放行**；本地后端直接拒绝（没有隔离还敢执行任意命令，风险太大）；
3. 容器参数里有断网、只读根文件系统、`cap_drop ALL`、禁止提权、**绝不挂载 `docker.sock`**
   等 13 项单元断言在管（见第 8 节）；
4. 但**降级到 local 时上面这些保障都不存在**：被测代码会以你的用户身份直接跑在宿主机上。
   只建议用来执行你自己生成的代码；装上 Docker 后设 `DOCKER__BACKEND=docker` 并拉一次镜像即可。

### 4.4 LLM 配置状态接口

写脚本、或者自己接前端时，先问一句"模型配好了吗"，比直接调生成接口再吃 503 更省事：

```powershell
$s = Invoke-RestMethod "$Base/api/v1/agent/status"
$s.configured        # 有没有可用的 Key（false 时生成类接口会返回 503）
$s.model             # 当前模型名
$s.base_url          # 实际请求的地址（排查"是不是发到别的服务商了"最快）
$s.provider          # 协议类型，如 openai-compatible
$s.timeout_seconds   # 超时
$s.max_retries       # 项目自己的重试次数
$s.prompt_version    # 提示词版本号：改了 Prompt 会变，便于对照结果差异
```

学生端页面用的是另一个状态接口 `GET /api/v1/tutor/status`，多了支持语言、上传上限与历史条数：

```powershell
$t = Invoke-RestMethod "$Base/api/v1/tutor/status"
$t.ai_available; $t.model; $t.supported_languages; $t.max_upload_bytes; $t.history_count
```

两个接口都**不会返回 Key 本身**，只回答"有没有配好"。

### 4.5 端到端脚本（一条命令看完整链路）

```powershell
# 离线演示：本地带 bug 的仓库 + 假 LLM，其余全真实（无需网络、无需 API Key）
python tests/e2e_test.py --offline
```

实测输出（11 个步骤）：

```
[1] 克隆与解析       仓库ID=1 状态=ready 文件=1 符号=1
[3] 构建检索索引     分块=1 维度=256 嵌入后端=hashing
[4] 混合检索         #1 add  calc.py  L1-3
[5] 生成测试         run_id=85665a... 用例=['test_add_sums_two_numbers', ...]
[6] 沙箱执行         exit=1 通过=0 失败=2 覆盖率=100.0%
                     [i] 用例失败符合预期：被测代码本身带 bug
[7] 自动修复         状态=passed 轮次=2
                     第1轮 exit=1 通过=0 失败=2 归类=source_bug 补丁=已应用
                     第2轮 exit=0 通过=2 失败=0
[8] PR 草稿          标题：fix: add 两个数相加结果是错的
                     分支：fix/agent-add-5e5ba0  verified=True

汇总：11/11 步通过
```

跑真实仓库：

```powershell
python tests/e2e_test.py `
  --repo "https://gitee.com/mirrors/requests.git" `
  --issue "检查状态码并在出错时抛出异常"
```

加 `--json` 输出机器可读报告，加 `--verbose` 同时打印应用自身日志。

### 4.6 真实仓库 + 真实模型实测（psf/requests）

配好 `LLM__API_KEY` 后跑通，**11/11 步通过**，`report.success=True`、脚本退出码 0：

```
[1] 克隆与解析      文件=37 符号=793 体积=7.3MB
[3] 构建检索索引    分块=793 维度=512 嵌入后端=fastembed
[4] 混合检索        #1 default_headers  src/requests/utils.py  L951-962   ← 中文查询命中正确文件
[5] 生成测试        模型=deepseek-flash 用例=4 个
[6] 沙箱执行        backend=local isolated=False 覆盖率=65.1%
[7] 自动修复        状态=passed success=True 轮次=1  通过=5 失败=0 错误=0
[8] PR 草稿         标题：fix: 修复 requests 中 utils.py 的无效转义序列警告  verified=True

汇总：11/11 步通过
```

两点如实说明：

1. 第 6 步的计数里**混有仓库自身用例**的结果——那个接口把生成的测试放进仓库的 `tests/` 目录并跑整个目录，
   而仓库的测试依赖（httpbin 等）并没有装进沙箱。看趋势可以，别当成"我的测试过没过"的判据。
   修复循环用的是隔离的 `agent_tests/`，不受影响。
2. 开发机没有 Docker，第 6、7 步走的是 local 后端（`isolated=False`），
   **没有隔离能力**，是"能跑"而不是"安全隔离"。

---

## 5. 配置与运行数据

配置全部写在项目根目录的 `.env` 里（`APP__XXX__YYY` 这种双下划线是"嵌套配置"的写法）。
改完 `.env` 要**重启服务**才生效。

### 5.1 大模型：唯一必须自己填的

```ini
# ---- 大模型：唯一必须自己填的 ----
LLM__BASE_URL=https://api.deepseek.com/v1   # 任何 OpenAI 兼容端点都行
LLM__API_KEY=                                # ← 填这里（.env 已被 .gitignore 忽略）
LLM__MODEL=deepseek-chat
# 实测说明：DeepSeek 会把请求的模型名统一路由到当前主力模型——
# 请求 deepseek-coder / deepseek-chat 返回的都是 model=deepseek-flash。
# 所以填哪个官方模型名都能跑；换其它服务商时按它的文档填。

# 用本地推理（vLLM / Ollama / LM Studio）就改成：
# LLM__BASE_URL=http://127.0.0.1:11434/v1    # Ollama 示例
# LLM__MODEL=qwen2.5-coder:7b
# LLM__API_KEY=not-needed                    # 本地服务通常不校验，但必须非空

# ---- 多模型：想在网页上切换模型（或让学生填自己的 Key）时加这一段 ----
# 写一组 LLM__MODELS__<ID>__* 就多一个可选模型，<ID> 用小写英文（deepseek / qwen / ollama）。
# 只有 MODEL_NAME 是必填：写了 PROVIDER 就能自动补出 API 地址；
# PROVIDER=ollama 这类本地模型会自动判定为"不需要 Key"。
LLM__MODELS__DEEPSEEK__LABEL=DeepSeek 官方
LLM__MODELS__DEEPSEEK__PROVIDER=deepseek
LLM__MODELS__DEEPSEEK__MODEL_NAME=deepseek-chat
LLM__MODELS__DEEPSEEK__API_KEY=              # 留空 = 用网页上输入的，或用下面的全局 Key 兜底
LLM__MODELS__QWEN__PROVIDER=qwen
LLM__MODELS__QWEN__MODEL_NAME=qwen-plus
LLM__MODELS__QWEN__API_KEY=
LLM__MODELS__OLLAMA__PROVIDER=ollama
LLM__MODELS__OLLAMA__MODEL_NAME=qwen2.5-coder:7b    # 本地模型，免 Key
LLM__DEFAULT_MODEL=deepseek                  # 默认用哪个（填上面的 <ID>）
```

**多模型与单模型的关系**（两种写法可以并存）：

| 情况 | 实际用哪个 |
| --- | --- |
| 配了 `LLM__MODELS__*` | 以多模型为准，网页上可以切换 |
| 一个多模型都没配 | 用 `LLM__MODEL` / `LLM__BASE_URL` 这一组（老配置照样能跑） |
| 多模型的某项没写 | 超时 / max_tokens / temperature 从全局 `LLM__*` 继承 |
| 某个云端模型没配 Key | 用全局 `LLM__API_KEY` 兜底（**本地模型不吃兜底**，免得把云端密钥发给本机服务） |
| 网页上填了 Key | 优先级最高，**只用于本次请求，不写数据库、不落盘** |

### 5.2 多模型：网页上切换模型 / 让使用者填自己的 Key

不想把自己的 Key 写进 `.env`、也不想让别人用你的 Key？让使用者**在网页上填**
（页面上的操作步骤见 [1.11](#111-模型设置在网页上选模型填自己的-api-key)，下面是脚本版）：

```powershell
$Base = "http://127.0.0.1:8000"

# 1) 看有哪些模型可选（响应里没有 Key，只有"后端配没配"）
Invoke-RestMethod "$Base/api/v1/models/list"

# 2) 存自己的 Key（Key 走请求体，不走 URL —— 查询串会被访问日志原样打印）
$r = Invoke-RestMethod "$Base/api/v1/auth/set_key" -Method Post -ContentType "application/json" `
  -Body (@{ model_id = "deepseek"; api_key = "sk-你的Key" } | ConvertTo-Json)
$session = $r.session.session_id      # 记住会话号，后续请求放在 X-Session-Id 头里
$r.message                            # 「已保存…30 分钟不用会自动清除」

# 3) 查状态（只回"有没有 Key"，不回 Key 本身）
Invoke-RestMethod "$Base/api/v1/auth/status" -Headers @{ "X-Session-Id" = $session }

# 4) 用完清掉（不带 model_id 清整个会话；带上就只清那一个模型）
Invoke-RestMethod "$Base/api/v1/auth/clear_key" -Method Post -ContentType "application/json" `
  -Body (@{ session_id = $session } | ConvertTo-Json)
```

**Key 是怎么被保护的**（这几条是设计上的硬约束，不是"尽量"）：

| 约束 | 做法 |
| --- | --- |
| 不落库 | 只放在进程内存的字典里，**进程重启即消失**；数据库里永远查不到 |
| 不进日志 | 日志只打会话号前 6 位 + 模型名 + 掩码 `sk-***`；连对象 `repr` 里都只有掩码 |
| 不回传 | 所有响应里都没有 `api_key` 字段，只有 `has_key: true/false` |
| 会过期 | 默认 **30 分钟无操作**自动清除，每次使用顺延 |
| 有上限 | 内存里最多 `SECURITY__MAX_SESSIONS`（默认 500）个会话，超出淘汰最久未用的 |
| 可关闭 | `SECURITY__ALLOW_CLIENT_KEY=false` 时该接口直接 403，只能用 `.env` 里配的 Key |

相关的三行 `.env` 配置：

```ini
SECURITY__API_KEY_TTL_SECONDS=1800   # 有效期（秒），默认 30 分钟，每次使用顺延
SECURITY__MAX_SESSIONS=500           # 内存里最多留多少个会话
SECURITY__ALLOW_CLIENT_KEY=true      # 设 false = 只允许用 .env 里配好的 Key
```

会话过期、被清除、Key 无效这三种情况都会让页面回到"未启用"：
徽章变回 `AI 未启用`、AI 按钮变灰，并提示「Key 已过期或被清除，请重新填写后再试」。

### 5.3 检索、沙箱、仓库、项目库、分类的配置

```ini
# ---- 检索：影响首次启动耗时 ----
RETRIEVAL__EMBEDDER=fastembed    # 真语义检索（默认，首次需下载约 180MB 模型）
                                 # 改成 hashing 则零下载、零依赖，但精度低
RETRIEVAL__HF_ENDPOINT=https://hf-mirror.com   # 国内网络走镜像
RETRIEVAL__HF_DISABLE_XET=true   # 必须为 true，否则镜像站下不动模型

# ---- 沙箱 ----
DOCKER__BACKEND=auto             # auto=有 Docker 就用，没有就降级 local
DOCKER__IMAGE=python:3.10-slim
DOCKER__INSTALL_DEPENDENCIES=true # 纯净镜像不含 pytest，需联网安装

# ---- 仓库安全边界 ----
REPOSITORY__ALLOWED_HOSTS=github.com,gitee.com  # 只允许从这些主机克隆

# ---- 本地项目库（学生代码练习台）----
LIBRARY__ROOTS=                  # 留空=只扫 data/library + 自带 examples
                                 # 想扫自己的作业：LIBRARY__ROOTS=D:\我的作业, E:\课程代码
LIBRARY__MAX_FILES=500           # 单次扫描最多收录多少个代码文件
LIBRARY__MAX_FILE_BYTES=512000   # 单文件超过 500KB 只登记元信息、不读内容
# LIBRARY__ALLOW_WRITE=true      # 取消注释并设为 false = 整体只读（页面上的编辑/替换/删除消失）
# LIBRARY__TRASH_DIR_NAME=.trash # 删除时先移到哪；设成空字符串 = 直接彻底删除、不留回收站

# ---- 本地代码文件自动分类（会把文件搬进 c/ java/ python/）----
CLASSIFIER__ROOT=data/library    # 待分类的根目录
CLASSIFIER__RECURSIVE=true       # 是否递归子目录
CLASSIFIER__MOVE_UNKNOWN=false   # 未知文件是否也搬到 unknown/（默认否：只登记不移动）
CLASSIFIER__MAX_FILES=2000
CLASSIFIER__MAX_FILE_BYTES=5242880
```

**关于密钥**：`LLM__API_KEY` 请只写在 `.env` 里（该文件已在 `.gitignore` 中）。
`.env.example` 是分发给别人的模板，**里面必须留空**——写真实 Key 等于把密钥公开，
写占位符（如 `sk-replace-me`）又会让服务误判为"已配置"、运行时才 401。

**关于项目库目录**：能被扫描的目录只能在后端 `.env` 里配置，页面与接口都无法指定任意路径，
且每次读写都会校验目标文件确实位于根目录之内。
写入类操作（在线编辑保存 / 替换 / 删除）还额外有三条约束：
只能改已存在的源码文件、删除默认进 `.trash` 回收站、`LIBRARY__ALLOW_WRITE=false` 可整体关掉。

**国内网络建议**：`RETRIEVAL__HF_ENDPOINT` 与 `RETRIEVAL__HF_DISABLE_XET` 两项保持默认即可，
它们正是为公网 huggingface.co 不可达的情况准备的。

### 5.4 全部配置项速查表

`.env.example` 是分发给别人的模板（Key 必须留空），下面按分组列出全部配置项。
**没提到的项保持默认即可**；写了说明的，都是实际踩过或测过的。

**应用与数据库**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `APP__NAME` / `APP__VERSION` / `APP__ENVIRONMENT` | opensource-collab-agent / 0.1.0 / local | 健康检查会回显这三项 |
| `APP__DEBUG` | true | 关掉后异常不再带调试信息 |
| `APP__HOST` / `APP__PORT` | 127.0.0.1 / 8000 | **只监听本机**；改端口时 `start.bat` 顶部的 `APP_PORT` 要一起改 |
| `APP__LOG_LEVEL` | INFO | 排查问题改 `DEBUG`，会打印每次请求的细节 |
| `APP__CORS_ORIGINS` | localhost:5173, 127.0.0.1:5173 | 单独用 Vite 起前端时才需要 |
| `APP__TRACE_HEADER` | X-Trace-Id | 响应头里回传链路号用的名字 |
| `DATABASE__SQLITE_PATH` | ./data/app.db | SQLite 文件位置 |
| `DATABASE__ECHO` | false | 打开后把每条 SQL 打进日志（调试用，很吵） |
| `DATABASE__URL_OVERRIDE` | 空 | 留空用 SQLite；填 `postgresql+asyncpg://…` 可切 PostgreSQL |

**大模型**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `LLM__PROVIDER` | openai-compatible | 只要兼容 `/chat/completions` 就能接 |
| `LLM__BASE_URL` | https://api.deepseek.com/v1 | 云端地址，或本地推理服务地址 |
| `LLM__API_KEY` | 空 | **留空是故意的**：接口会明确说"未配置"，而不是拿假 Key 去 401 |
| `LLM__MODEL` | deepseek-chat | 模型名 |
| `LLM__TIMEOUT_SECONDS` | 120 | 单次请求超时 |
| `LLM__MAX_TOKENS` | 4096 | 生成上限（要它写长代码/长讲解时可以调大） |
| `LLM__TEMPERATURE` | 0.2 | 越低越稳，代码任务建议保持低值 |
| `LLM__MAX_RETRIES` | 2 | 项目自己的重试次数（SDK 自带重试已关闭，避免叠加） |
| `LLM__RETRY_BACKOFF_SECONDS` | 1.0 | 重试退避基数（指数增长） |
| `LLM__MODELS__<ID>__*` | — | 一个模型一组：`LABEL` / `PROVIDER` / `MODEL_NAME`（必填）/ `API_KEY` / `BASE_URL` / `DESCRIPTION` / `API_KEY_ENV` / `ENABLED` |
| `LLM__DEFAULT_MODEL` | — | 默认用哪个 `<ID>`；留空用第一个 |

**安全（网页填 Key 的内存保管箱）**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `SECURITY__API_KEY_TTL_SECONDS` | 1800 | 无操作多久自动清除 |
| `SECURITY__MAX_SESSIONS` | 500 | 内存里最多留几个会话，超出淘汰最久未用的 |
| `SECURITY__ALLOW_CLIENT_KEY` | true | 设 false → 网页填 Key 返回 403，只能用 `.env` 里的 |

**检索**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `RETRIEVAL__VECTOR_BACKEND` | faiss | 向量库后端 |
| `RETRIEVAL__INDEX_DIR` | ./data/index | 索引存放位置 |
| `RETRIEVAL__EMBEDDER` | fastembed | `hashing` = 零依赖确定性哈希（离线/CI 兜底，精度低） |
| `RETRIEVAL__EMBEDDING_MODEL` | BAAI/bge-small-zh-v1.5 | 换 `intfloat/multilingual-e5-large` 可改善跨语言 |
| `RETRIEVAL__EMBEDDING_DIM` | 512 | **必须与模型匹配**：e5-large 要改成 1024 |
| `RETRIEVAL__MODEL_CACHE_DIR` | ./data/models | 模型缓存目录 |
| `RETRIEVAL__HF_ENDPOINT` | https://hf-mirror.com | 国内镜像 |
| `RETRIEVAL__HF_DISABLE_XET` | true | 镜像不代理 Xet 协议，必须关 |
| `RETRIEVAL__TOP_K` | 10 | 默认返回条数 |
| `RETRIEVAL__CANDIDATE_MULTIPLIER` | 3 | 每路召回的候选倍数 |
| `RETRIEVAL__BM25_WEIGHT` / `VECTOR_WEIGHT` | 0.5 / 0.5 | 关键词与向量的权重 |
| `RETRIEVAL__RRF_K` | 60 | RRF 融合参数 |
| `RETRIEVAL__MAX_CHUNK_CHARS` | 2000 | 单个代码块最大字符数 |

**沙箱**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `DOCKER__BACKEND` | auto | auto / docker / local |
| `DOCKER__IMAGE` / `BASE_IMAGE` | python:3.10-slim | 沙箱镜像（可用 `docker/sandbox.Dockerfile` 预构建） |
| `DOCKER__INSTALL_DEPENDENCIES` | true | 纯净镜像不含 pytest/coverage，需联网安装；预构建镜像后设 false |
| `DOCKER__PIP_INDEX_URL` | 空 | 国内可指向镜像源 |
| `DOCKER__SANDBOX_USER` | 空 | 建议设非 root（如 1000:1000） |
| `DOCKER__TIMEOUT_SECONDS` | 300 | 执行超时 |
| `DOCKER__MEMORY_LIMIT` / `CPU_LIMIT` / `PIDS_LIMIT` | 1g / 2.0 / 256 | 资源上限 |
| `DOCKER__NETWORK_DISABLED` | true | 断网；装了依赖时网络其实是通的，接口会如实标 `network_actually_disabled` |
| `DOCKER__READ_ONLY_ROOT` / `TMPFS_SIZE` | true / 64m | 只读根文件系统 + 可写临时空间 |
| `DOCKER__WORKSPACE_DIR` / `WORKSPACE_MOUNT` | ./data/workspaces / /workspace | 工作区与容器内挂载点（**沙箱接口的路径边界就是它**） |
| `DOCKER__MAX_OUTPUT_BYTES` | 1000000 | 输出上限，超了 `truncated=true` |
| `DOCKER__TEST_DIR` | tests | 默认测试目录名 |

**Trace / 仓库 / 项目库 / 分类**

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `TRACING__ENABLED` / `SINK` | true / log | 关掉就不再记录链路 |
| `TRACING__CAPTURE_INPUT` / `CAPTURE_OUTPUT` | true / true | 是否把输入输出记进 trace（含代码片段，注意隐私） |
| `TRACING__MAX_PAYLOAD_CHARS` | 2000 | 单条 payload 记录上限 |
| `TRACING__MEMORY_BUFFER_SIZE` | 1000 | 内存里保留多少条 |
| `REPOSITORY__WORKSPACE_DIR` | ./data/repos | 克隆下来的仓库放哪 |
| `REPOSITORY__CLONE_DEPTH` | 1 | 浅克隆深度；0 = 完整克隆（要 git 历史时才用） |
| `REPOSITORY__CLONE_TIMEOUT_SECONDS` | 300 | 克隆超时 |
| `REPOSITORY__ALLOWED_HOSTS` | github.com,gitee.com | **只允许从这些主机克隆**，挡掉 `file://` 与 SSRF |
| `REPOSITORY__MAX_REPO_SIZE_MB` / `MAX_FILES` / `MAX_FILE_BYTES` | 500 / 5000 / 512000 | 仓库体积、文件数与单文件上限 |
| `REPOSITORY__EXCLUDED_DIRS` | .git,node_modules,__pycache__,.venv,… | 解析时跳过的目录 |
| `LIBRARY__ROOTS` | 空 | 留空 = `data/library` + 自带 `examples` |
| `LIBRARY__MAX_DEPTH` / `MAX_FILES` / `MAX_FILE_BYTES` | 6 / 500 / 512000 | 见 [2.6](#26-项目库的状态与上限) |
| `LIBRARY__INCLUDE_EXAMPLES` | true | 是否扫描自带示例目录 |
| `LIBRARY__ALLOW_WRITE` | true | false = 只读（页面上的编辑/替换/删除消失） |
| `LIBRARY__TRASH_DIR_NAME` | .trash | 回收站目录名；空字符串 = 不留回收站 |
| `CLASSIFIER__ROOT` | data/library | 归档根目录（`/files/scan` 只允许扫它及子目录） |
| `CLASSIFIER__RECURSIVE` | true | 是否递归子目录 |
| `CLASSIFIER__MOVE_UNKNOWN` | false | 未知类型是否也搬到 `unknown/`（默认只登记不移动） |
| `CLASSIFIER__MAX_FILES` / `MAX_FILE_BYTES` | 2000 / 5242880 | 单次处理上限 |
| `CLASSIFIER__EXCLUDED_DIRS` | .git,node_modules,… | 跳过目录；**`.trash` 会被自动追加**，免得把删掉的文件又扫回来 |

### 5.5 运行数据都存在哪

| 路径 | 存什么 | 能不能删 |
| --- | --- | --- |
| `data/app.db` | SQLite 数据库（8 张表，见下） | 可以删，等于清空历史与台账 |
| `data/index/` | 检索索引（BM25 + FAISS） | 可以删，下次建索引会重建 |
| `data/repos/` | 克隆下来的仓库副本（只读） | 可以删，但已注册的仓库要重新克隆 |
| `data/workspaces/` | 沙箱工作区：生成的测试、修复用的隔离副本 | 可以删（修复产物一起没了） |
| `data/library/` | **学生作业本体** + `.trash/` 回收站 | **别删**，这是学生自己的文件 |
| `data/models/` | 嵌入模型缓存（约 180MB） | 可以删，但下次要重新下载 |
| `examples/示例作业/` | 项目自带的、故意写错的示例代码 | 属于项目文件，不是运行数据 |

8 张业务表分别存什么：

| 表 | 一条记录代表什么 |
| --- | --- |
| `repositories` | 注册过的开源仓库（URL / 本地副本 / 文件数 / 符号数） |
| `code_files` | 仓库里的一个代码文件（语言 / 行数 / 路径） |
| `code_symbols` | 一个函数或类（签名 / 起止行 / 圈复杂度 / docstring） |
| `tutor_records` | 学生端的一次操作（分析 / 检测 / 注释 / 改错）+ 当时的代码 |
| `classified_files` | 归档台账里的一条（原名 / 归档路径 / 语言 / 大小 / 是否还在磁盘上） |
| `code_check_records` | 一次 AI 检测（评分 / 等级 / 四类问题数） |
| `code_fix_records` | 一次改错（原代码 / 新代码 / 逐条讲解 / 复检结论） |
| `comment_records` | 一次注释生成（原代码 / 带注释代码 / 覆盖情况） |

想直接查库（比如确认学生到底交了几份）：

```powershell
python -c "import sqlite3; c=sqlite3.connect('data/app.db'); print(c.execute('select action, count(*) from tutor_records group by action').fetchall())"
```

### 5.6 出问题怎么定位：Trace、日志与状态码

**每个响应都带链路号**：看响应头 `X-Trace-Id`（名字由 `APP__TRACE_HEADER` 决定），
返回体里也常有 `trace_id` 字段。拿着它去日志里搜，一次请求跨了哪些阶段、
每阶段花了多久、报了什么错，全都串得起来。

```powershell
$r = Invoke-WebRequest "$Base/api/v1/health"      # 想看响应头得用 Invoke-WebRequest
$r.Headers["X-Trace-Id"]
```

**日志在哪看**：`start.bat` 把服务跑在前台窗口里，日志直接打在那个窗口；命令行启动就是终端输出。
想看更细的就把 `APP__DEBUG` 设为 `true`、`APP__LOG_LEVEL` 设为 `DEBUG`。
日志里**永远不会出现完整的 API Key**，只有掩码 `sk-***`。

**按状态码对号入座**（各模块约定一致，遇到先看这张表）：

| 状态码 | 含义 | 常见于 |
| --- | --- | --- |
| `400` | 请求不合法：路径越界、参数不对、命令被拒 | 项目库读写、沙箱 `command`、仓库 URL |
| `403` | 被安全开关拒绝 | `LIBRARY__ALLOW_WRITE=false`、`SECURITY__ALLOW_CLIENT_KEY=false` |
| `404` | 找不到对象 | 仓库 ID、历史记录 ID、文件不存在 |
| `409` | 状态冲突 | 乐观锁（文件被别处改过）、同名文件已存在、索引缺失、本地副本丢失 |
| `413` | 内容太大 | 单文件超过上限 |
| `422` | 参数校验失败 / 生成结果不可用 | 请求体字段不合法、生成的测试提取失败 |
| `502` | 上游出错 | 模型服务返回错误、克隆失败、沙箱执行异常 |
| `503` | 服务端未就绪 | 没配 `LLM__API_KEY`、Docker 不可用 |
| `504` | 上游超时 | 模型超时 |
| `200` + `ai_available=false` | **不是错误**：没配模型，只给了本地分析结论 | 检测 / 改错 / 注释 |

---

## 6. 常见问题

**Q：启动后 AI 按钮是灰的 / 提示"AI 未启用"？**
没配 `LLM__API_KEY`。这是刻意设计：返回 503 并说明原因，而不是拿假 Key 去 401。
填好 `.env` 后重启服务即可。**没配也能用**——本地项目库、文件分类、在线编辑，
以及检测/改错/注释的本地分析部分都照常工作。
也可以在页面上「模型设置」里填自己的 Key（见 [1.11](#111-模型设置在网页上选模型填自己的-api-key)）。

**Q：模型下拉框里只有一个模型（比如只有 DeepSeek），想换成 Qwen / 本地 Ollama？**
说明 `.env` 里**没有写多模型那一段**——页面显示的其实是"单模型写法"的兜底项，
它的说明文字会点出来：`来自单模型配置（LLM__MODEL / LLM__BASE_URL / LLM__API_KEY）`。
这不是"项目只支持一个模型"：`LLM__MODEL` 填什么，它就是什么。

把下面这段加进 `.env`（`LLM__MODELS__*` 一次可以写多个），重启服务，
再点模型卡片标题右边的 ↻，下拉框里就会多出这些模型：

```ini
LLM__MODELS__DEEPSEEK__LABEL=DeepSeek 官方
LLM__MODELS__DEEPSEEK__PROVIDER=deepseek
LLM__MODELS__DEEPSEEK__MODEL_NAME=deepseek-chat

LLM__MODELS__QWEN__LABEL=通义千问 Qwen
LLM__MODELS__QWEN__PROVIDER=qwen
LLM__MODELS__QWEN__MODEL_NAME=qwen-plus

LLM__MODELS__OLLAMA__LABEL=本地 Ollama（免 Key）
LLM__MODELS__OLLAMA__PROVIDER=ollama
LLM__MODELS__OLLAMA__MODEL_NAME=qwen2.5-coder:7b

LLM__DEFAULT_MODEL=deepseek
```

三个要点：**只有 `MODEL_NAME` 是必填的**（写了 `PROVIDER` 会自动补 API 地址）；
每个模型可以各配一把自己的 Key（`LLM__MODELS__<ID>__API_KEY`）；
本地模型（`PROVIDER=ollama`）自动判定为免 Key。详见 [5.2](#52-多模型网页上切换模型--让使用者填自己的-key)。

**Q：双击 `start.bat` 窗口一闪就没了？**
一般是 Python 没装或没加入 PATH。装 Python 3.11/3.12 并勾选 "Add Python to PATH" 即可。

**Q：端口 8000 被占用？**
双击 `stop.bat` 释放，或改 `.env` 里的 `APP__PORT`（同时改 `start.bat` 顶部的 `APP_PORT`）。

**Q：删掉的文件去哪了？**
`data/library/.trash/`，名字形如 `20260218-153000__python__homework1.py`。
把名字改回去、移出 `.trash` 就恢复了。不想要回收站就把 `LIBRARY__TRASH_DIR_NAME` 设成空字符串。

**Q：保存时报 409「文件在别处被改动过」？**
你打开这个文件之后，它在别处被改过（记事本、另一个编辑器、Git 切分支）。
页面记的是打开那一刻的指纹，为了不覆盖新内容会拒绝保存——重新打开文件再改即可。

**Q：首次启动很慢 / 卡在下载模型？**
首次建索引时会下载嵌入模型（约 180MB）。想跳过就把 `RETRIEVAL__EMBEDDER=hashing`，
检索精度会下降但完全离线可用。

**Q：中文搜英文代码效果不好？**
默认模型 `bge-small-zh-v1.5` 在"中文查询→中文文档"上 rank-1 接近 100%，
但"中文查询→纯英文文档"约 40%——这是小模型的客观限制。
跨语言要求高时换成 `RETRIEVAL__EMBEDDING_MODEL=intfloat/multilingual-e5-large`
（同时把 `RETRIEVAL__EMBEDDING_DIM` 改成 1024）。
另外查询里带上英文标识符（如 `raise_for_status 检查状态码`）命中率会明显更高。

**Q：`/sandbox/status` 显示 `isolated: false`？**
没有可用的 Docker daemon，沙箱降级成了本地子进程——**没有隔离能力**，
被测代码会以你的用户身份直接跑在宿主机上。只建议用来执行自己生成的代码。
装上 Docker 后设 `DOCKER__BACKEND=docker` 即可。

**Q：`auto_fix` 返回 `status=max_attempts` 或 `no_patch`？**
看 `iterations[].analysis`，里面是修复 Agent 给出的失败根因分析。
- `no_patch`：模型没能给出可用的 diff（常见于日志信息不足）
- `not_patchable`：判定为环境问题或代码本来就正确，按设计不出补丁
- `no_progress`：改了代码但失败特征完全没变，提前终止（不浪费剩余轮次）

**Q：Windows 上 `curl` 发的 JSON 报 `JSON decode error`？**
见 [3.2](#32-curllinux--macos--git-bash) 下面的提示：用 `Invoke-RestMethod`。

**Q：怎么彻底清理运行数据？**
删掉 `data/` 下的 `app.db`、`index/`、`repos/`、`workspaces/` 即可（`data/models/` 是模型缓存，
删了下次要重新下载）。**学生作业在 `data/library/`，别误删。**

---

## 7. 开发与测试

```powershell
python -m pytest                 # 1112 个用例，全部离线可跑，约 4 分钟
python -m ruff check app tests tools   # 代码检查（当前 0 问题）
```

**验收脚本（16 个，全部会真的起服务、发 HTTP 请求；下面每一项都实际跑过）**：

| 脚本 | 覆盖内容 | 实测 |
| --- | --- | --- |
| `python tools\verify_integration.py` | 整合自检：模块注册 / 依赖一致 / README 接口与章节 / 文档内部链接 / .bat 与数据库表 | 60 项 ✓ |
| `python tools\verify_frontend.py` | 页面与静态资源 + tutor + library 接口 | 20 项 ✓ |
| `python tools\verify_dragdrop.py` | 拖拽入库 / 占位提示 / 分隔条 / 大文件懒加载 | 39 项 ✓ |
| `python tools\verify_edit_delete.py` | 在线编辑 / 替换 / 删除 / 回收站 / 只读模式 | 45 项 ✓ |
| `python tools\verify_files.py` | 文件自动分类：扫描 / 归档 / 查询 / 边界 | 11 项 ✓ |
| `python tools\verify_check.py` `[--ai]` | AI 检测：本地检查路径 9 项；加测真实模型 12 项 | ✓ |
| `python tools\verify_fix.py` `[--ai]` | 代码改错：本地分析 9 项；加测真实模型 12 项 | ✓ |
| `python tools\verify_comment.py` `[--ai]` | 注释生成：本地路径 6 项；加测真实模型 19 项 | ✓ |
| `python tools\verify_bat.py` | `start.bat` → 服务 → `stop.bat` 全流程 | 9 项 ✓ |
| `python tools\check_frontend.py` | JS/DOM/CSS/接口交叉检查 + 前端行为测试 | 行为 **135 项** ✓ |
| `python tools\verify_ai_real.py` | 真实大模型验收（需要 `.env` 配好 Key，会真的调用模型） | 10 项 ✓ |
| `python tools\verify_llm_client_real.py` | 通用 LLM 客户端：调用 / 流式 / Key 无效 / Key 为空 | 14 项 ✓ |
| `python tools\verify_model_switch.py` | 模型选择 + 网页填 Key 全链路（含 Key 无效与清除） | 18 项 ✓（`.env` 没真 Key 时 12 项） |
| `python tools\audit_dead_code.py` | 死代码审计（只读）：没人引用的模块 / 函数 / CSS 类名 + 多余产物 | 0 处 ✓ |
| `python tools\build_bat.py` | **生成** `start.bat` / `stop.bat`（改过脚本内容就跑它重新生成，GBK + CRLF） | — |
| `python tools\check_frontend_logic.mjs` | 前端行为测试本体（Node + DOM 桩，真跑 `app.js`）；由 `check_frontend.py` 第 5 步自动调用 | 135 项 ✓ |

加 `--ai` 的那三个脚本会真的联网调用模型（约 2-5 秒/次），不加就只跑本地路径：

```powershell
python tools\verify_check.py --ai
python tools\verify_fix.py --ai
python tools\verify_comment.py --ai
```

这些脚本**不需要你手动先启动服务**：除 `check_frontend.py`（静态检查 + Node 行为测试）之外，
其余会自己拉起临时 uvicorn（独立端口 + 独立临时数据目录），跑完自己关掉，不会碰你的 `data/`。

这些脚本不需要你手动先启动服务：除 `check_frontend.py`（静态检查 + Node 行为测试）之外，
其余会自己拉起临时 uvicorn（独立端口 + 独立临时数据目录），跑完自己关掉，不会碰你的 `data/`。

**测试设计**：不依赖网络、不依赖 API Key、不依赖 Docker——
克隆用本地构造的 git 仓库，检索用确定性哈希嵌入，LLM 用可注入的假客户端，沙箱真实执行 pytest 与 coverage。
（测试夹具会强制把 `LLM__API_KEY` 置空，避免开发机上配了真实 Key 时用例偷偷发请求出去。）

---

## 8. 能力边界（如实说明）

这几点是当前实现的真实限制，不是配置问题：

1. **容器隔离未在无 Docker 的环境验证**。代码里 Docker 路径的容器参数有 13 项单元断言覆盖
   （不挂 docker.sock、`cap_drop ALL`、只读根文件系统等），但"容器真能跑起来"需要在有 Docker 的机器上复验。
   开发机上没有 Docker daemon，沙箱实际走的是 local 后端（接口会如实标注 `isolated: false`）。
2. **检索的跨语言能力是短板**，见上面 Q&A。
3. **克隆/索引/生成/执行都在请求内同步完成**，大仓库或慢模型下请求会比较久。
   架构上应该改成"返回 202 + task_id，后台执行"。
4. **三个 AI 功能没有单元测试覆盖真实模型**：`tests/` 里用假客户端验证代码路径，
   真实效果由 `tools/verify_ai_real.py` 人工验收（实测 10/10 项通过，见下表）。

### 真实模型验收记录（`tools/verify_ai_real.py`）

示例作业在 `examples/示例作业/`，每个文件都预先埋了错误，用来判断模型有没有真找对：

| 文件 | AI 检测 | 生成注释 | 自动改错 |
| --- | --- | --- | --- |
| `bubble_sort.c` | 55 分/待改进，3 个问题：越界访问、比较符号写反、数组长度硬编码；2 个预设 bug 全部命中 | 56 行，中文 388 字，原代码行保留 24/24 | 2 处修改，`i <= n`→`i < n`、`<`→`>` 均正确 |
| `score_stats.py` | 55 分/待改进，2 个问题：分母写错、空列表 IndexError；2 个预设 bug 全部命中 | 47 行，中文 364 字，原代码行保留 23/23 | 2 处修改，分母改为 `len(scores)`、补空列表判断 |
| `ScoreManager.java` | 78 分/良好，3 个问题：`max=0` 初始值、空数组隐患、构造方法未防御性拷贝 | 63 行，中文 358 字，原代码行保留 34/34 | 1 处修改，`max` 改用首元素初始化并处理空数组 |

单次调用耗时 2–5 秒；9 次调用全部成功写入历史记录。
**说明**：这只覆盖了 3 个示例文件，不足以代表"任意学生代码都可靠"，
真实课堂使用前建议再拿一批真实作业抽样复验。
