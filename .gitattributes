# ---------------------------------------------------------------------------
# Git 属性：保证"跨平台克隆后仍然能跑"
# ---------------------------------------------------------------------------
# 背景（这个项目特有的硬要求）：
#   start.bat / stop.bat 是 **GBK 编码 + CRLF 换行**，而且 chcp 必须出现在
#   任何中文之前——cmd.exe 是用「执行到 chcp 那一刻的代码页」解析整个批处理
#   文件的。如果换行符被转成裸 LF，cmd 会丢掉 REM 前缀并刷屏报错
#   「不是内部或外部命令」。tools/verify_integration.py 里有对应断言。
#
# 所以：批处理文件声明为"文本 + 检出时必须是 CRLF"，这样即使在 Linux/macOS 上
# 克隆，拿到的也是 cmd.exe 能认的 CRLF。
*.bat text eol=crlf

# 生成 .bat 用的 UTF-8 模板：两个模板的换行符并不一致（start 是 CRLF、stop 是 LF），
# 而 tools/build_bat.py 生成时会统一成 CRLF，所以这里不做任何转换，避免无谓的改动噪音。
*.bat.utf8 -text

# 二进制文档不要做换行符转换
*.docx binary
*.png binary
*.jpg binary
*.ico binary
